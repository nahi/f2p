<p>Icons used in this site are <a href="http://www.famfamfam.com/lab/icons/silk/">&quot;Silk&quot; by FAMFAMFAM</a>.  Thanks!</p>

<hr />

<div class="listings">
<h2>Top Menu</h2>
<ul>
  <li><%= icon_tag(:write) %> ... add new entry</li>
  <li><%= icon_tag(:search) %> ... search entries</li>
  <li><%= icon_tag(:settings) %> ... change session settings</li>
  <li><%= icon_tag(:help) %> ... show this help</li>
  <li><%= icon_tag(:logout) %> ... logout and discard settings</li>
</ul>

<h2>Entry Edit Operation</h2>

<ul>
  <li><%= icon_tag(:like) %> ... add 'like'</li>
  <li><%= link_to(h("(01:23)")) %> ... show comment form</li>
  <li><%= icon_tag(:star) %> ... show who 'like'd it; delete your 'like' once you 'like'd it</li>
</ul>

<h2>Page Menu</h2>
<ul>
  <li>[inbox] ... show your entries in 'home' excluding archived entries</li>
  <li>[next] ... start showing page-by-page view</li>
  <li>[all] ... show all of your entries in 'home' including archived entries</li>
  <li>[pin] ... show pinned entries</li>
  <li><%= icon_tag(:group) %> ... show group entries</li>
  <li>(user name) ... show user profile</li>
</ul>

<h2>Pin Operation</h2>

<ul>
  <li><%= icon_tag(:pin) %> ... add pin to the entry; pinned entries keep on showing in [inbox] and [pin]</li>
  <li><%= icon_tag(:pinned) %> ... unpin the entry and archive it</li>
  <li><%= submit_tag('mark as read') %> ... archive entries except pinned entries</li>
</ul>

<h2>Page Cursor</h2>
<ul>
  <li><%= icon_tag(:top) %> ... go top of the page</li>
  <li><%= icon_tag(:more) %> ... go detailed page which shows omitted entries/comments of a list</li>
  <li><%= icon_tag(:bottom) %> ... go bottom of the page</li>
</ul>

<h2>User Filter</h2>

<p>These filters are shown at the top of user profile page.</p>

<ul>
  <li>[self] ... show entries of the user</li>
  <li>[discussion] ... show entries of the user which is commented or 'like'd</li>
  <li>[likes] ... show entries the user 'like'd</li>
  <li>[liked] ... show entries of the user which is commented by other</li>
  <li>[with friends] ... show entries of the user + his/her friends</li>
</ul>

</div>

<hr />

<p>
'f2p' is a web-based FriendFeed application for mobile users by NaHi.
It's based on <a href="http://friendfeed.com/api/">FriendFeed API</a> but
it's not formally affiliated with <a href="http://friendfeed.com">FriendFeed</a>.
</p>
<ul>
  <li>NO WARRANTY; This service is provided on an "AS IS" basis without warranty of any kind.</li>
  <li>Nickname and remote key are stored in DB of this server for accessing FriendFeed API.</li>
  <li>Your feed ID (e.g. 9ac57155-b878-4438-a0ab-c214d27b747d), pin state and its modification time are stored in DB, too.  Other feed content is NOT stored in DB.</li>
  <li>Let me know about any trouble on this service at <%= link_url('http://friendfeed.com/f2p') %>.</li>
</ul>
<hr />
<p>NAKAMURA, Hiroshi a.k.a. NaHi - <%= link_url('http://friendfeed.com/nahi') %></p>
