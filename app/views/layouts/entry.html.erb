<html>
  <head>
    <title><%= appname %> entries</title>
    <%= inline_meta -%>
    <%= inline_stylesheet -%>
  </head>
  <body>
    <%- form_tag({:action => 'list'}) do -%>
      <h1><%= link_to(h(appname), :controller => 'entry') %> <%= h(viewname) %> at <%= title_date %> <%= top_menu %> <%= search_form(:compact => true) %></h1>
    <%- end -%>
    <%= yield %>
  </body>
</html>
