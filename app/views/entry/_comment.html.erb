<%- if comment.respond_to?(:fold_entries) -%>
  <%= fold_comment_link(comment) %>
<%- else -%>
  <%= comment_icon(comment.by_user(v(comment.entry, 'user', 'nickname'))) %>
  <%= date(v(comment, 'date'), false) if ctx.eid %>
  <%= comment(comment) %>
  by <%= user(comment) %>
  <%= via(comment) -%>
  <%= date(v(comment, 'date'), true) unless ctx.eid %>
  <%= comment_url_link(comment) -%>
<%= delete_comment_link(comment) -%>
<%= post_comment_link(comment.entry) if !ctx.eid and comment == comment.entry.comments.last -%>
<%- end -%>
